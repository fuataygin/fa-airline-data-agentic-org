# This package holds FORGE (the Maker agent)'s generated artefact.
#
# products/bers_engine.py is NOT hand-written by a human — it is generated
# fresh, at pipeline run time, by the Maker agent, and then written here by
# pipeline.py so it can be executed and (optionally) committed as a real,
# reviewable deliverable.
